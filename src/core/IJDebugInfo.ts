module jasper.core {
    /**
     * Data structure for jDebug
     */
    export interface IJDebugInfo {
        /**
         * System path to the component/service etc.
         */
        path: string;
    }
}